import PropertyTable from '../../components/propertyTable';
import statusStickerSchema from '../../../../api/components/statusSticker.schema.json';

# StatusSticker

This component is a status based sticker. It can take 4 different statuses, `success`, `warning`, `error` and `pending`

## Examples

### Simple Status Sticker

```json
{
    "type": "statusSticker",
    "status": "success"
}
```

<PropertyTable schema={statusStickerSchema}/>
